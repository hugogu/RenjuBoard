<Window x:Class="RenjuBoard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:interaction="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:control="clr-namespace:RenjuBoard.Controls"
        xmlns:vm="clr-namespace:RenjuBoard.ViewModels"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        Title="Renju Board"
        Height="500" Width="960">
    <Window.Resources>
        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Margin" Value="0,5,0,0" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="380" />
        </Grid.ColumnDefinitions>
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom"
                        Margin="5">
                <TextBlock Text="Black" FontWeight="Black" />
                <TextBlock>
                    Used time: <Run Text="{Binding BlackTime, Mode=OneWay, StringFormat=\{0:hh\\:mm\\:ss\}}" />
                </TextBlock>
                <Separator />
                <TextBlock Text="White" FontWeight="Black" />
                <TextBlock>
                    Used time: <Run Text="{Binding WhiteTime, Mode=OneWay, StringFormat={}{0:hh}:{0:mm}:{0:ss}}"/>
                </TextBlock>
            </StackPanel>
            <StackPanel Margin="5">
                <Button Content="New Game"
                        Click="OnNewGameButtonClick" />
                <Button Content="Undo"
                        Command="{Binding UndoCommand}" />
                <Button Content="Redo"
                        Command="{Binding RedoCommand}" />
                <Separator Margin="0,5,0,0" />
                <Button Content="Save..."
                        Command="{Binding SaveCommand}" />
                <Button Content="Load..."
                        Command="{Binding LoadCommand}" />
                <Separator Margin="0,5,0,0" />
                <Button Content="Options"
                        Command="{Binding ShowOptionsCommand}" />
                <CheckBox Content="AI first" IsChecked="{Binding AIFirst}" />
            </StackPanel>
        </DockPanel>
        <DockPanel Grid.Column="1">
            <ToolBar DockPanel.Dock="Top" DataContext="{Binding AIControllerVM}">
                <Button Content="Pause" Command="{Binding PauseAICommand}" />
                <Button Content="Continue" Command="{Binding ContinueAICommand}" />
                <Button Content="Next" Command="{Binding NextAIStepComand}" />
                <Separator />
                <ToggleButton Content="Pause on start" IsChecked="{Binding PauseOnStart}" />
            </ToolBar>
            <control:GameBoardPanel ItemsSource="{Binding Points}"
                                    Lines="{Binding Lines}"
                                    PreviewLines="{Binding PreviewLines}"
                                    ResolvingPoints="{Binding ResolvingPoints}"
                                    ShowCalibrationLabels="{Binding OptionsVM.ShowCalibrationLabels}"
                                    Size="{Binding BoardSize}">
                <interaction:Interaction.Triggers>
                    <interaction:EventTrigger EventName="MouseLeave">
                        <interaction:InvokeCommandAction Command="{Binding ClearPreviewLinesCommand}" />
                    </interaction:EventTrigger>
                </interaction:Interaction.Triggers>
            </control:GameBoardPanel>
        </DockPanel>
        <GridSplitter Grid.Column="2"
                      Width="5"
                      HorizontalAlignment="Stretch" />
        <ContentControl Grid.Column="3">
            <vm:LogsViewModel />
        </ContentControl>
    </Grid>
</Window>
